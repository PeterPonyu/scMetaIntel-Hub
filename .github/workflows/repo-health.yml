name: repo-health

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install package metadata and public-test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy requests rich qdrant-client transformers
          python -m pip install -e . --no-deps

      - name: Syntax sweep
        run: python -m compileall scmetaintel geodh benchmarks scripts tests

      - name: Public contract tests
        run: python -m unittest discover -s tests -p 'test_*.py' -v
